import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    table: {
      minWidth: 650,
      maxWidth: 1200,
      margin: 'auto'
    },
    container: {
      display: 'block'
    }
  });